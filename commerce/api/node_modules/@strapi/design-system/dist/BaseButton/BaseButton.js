import { jsx as s } from "react/jsx-runtime";
import a from "react";
import d from "styled-components";
import { buttonFocusStyle as i } from "../themes/utils.js";
const n = d.button`
  display: flex;
  cursor: pointer;
  padding: ${({
  theme: r
}) => r.spaces[2]};
  border-radius: ${({
  theme: r
}) => r.borderRadius};
  background: ${({
  theme: r
}) => r.colors.neutral0};
  border: 1px solid ${({
  theme: r
}) => r.colors.neutral200};
  svg {
    height: ${({
  theme: r
}) => r.spaces[3]};
    width: ${({
  theme: r
}) => r.spaces[3]};
  }
  svg {
    > g,
    path {
      fill: ${({
  theme: r
}) => r.colors.neutral0};
    }
  }
  &[aria-disabled='true'] {
    pointer-events: none;
  }

  ${i}
`, l = a.forwardRef(({
  disabled: r,
  children: t,
  ...e
}, o) => s(n, {
  ref: o,
  "aria-disabled": r,
  type: "button",
  disabled: r,
  ...e,
  children: t
}));
l.displayName = "BaseButton";
export {
  l as BaseButton,
  n as BaseButtonWrapper
};
