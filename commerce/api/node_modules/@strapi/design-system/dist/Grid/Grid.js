import t from "react";
import a from "styled-components";
import e from "prop-types";
import { GridContext as i } from "./GridContext.js";
import { Box as n } from "../Box/Box.js";
import d from "../helpers/handleResponsiveValues.js";
const l = a(n)`
  display: grid;
  grid-template-columns: repeat(${({ gridCols: r }) => r}, 1fr);
  ${({ theme: r, gap: o }) => d("gap", o, r)}
`, p = ({ gap: r, gridCols: o, ...m }) => /* @__PURE__ */ t.createElement(i.Provider, {
  value: { gap: r, gridCols: o }
}, /* @__PURE__ */ t.createElement(l, {
  gap: r,
  gridCols: o,
  ...m
}));
p.defaultProps = {
  gap: 0,
  gridCols: 12
};
p.propTypes = {
  gap: e.oneOfType([e.number, e.arrayOf(e.number)]),
  gridCols: e.number
};
export {
  p as Grid
};
